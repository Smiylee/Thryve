<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Add Nurse</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        :root{
            --bg1: #f6f9ff;
            --bg2: #eef6ff;
            --card: #ffffff;
            --muted: #6b7280;
            --accent1: #6C5CE7;
            --accent2: #00B4D8;
            --danger: #ef4444;
            --radius: 12px;
        }

        *{box-sizing:border-box}
        html,body{
            height:100%;
            margin:0;
            font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
            background: linear-gradient(180deg,var(--bg1),var(--bg2));
            -webkit-font-smoothing:antialiased;
            -moz-osx-font-smoothing:grayscale;
            color:#0f172a;
        }

        .wrap{
            min-height:100%;
            display:flex;
            align-items:center;
            justify-content:center;
            padding:40px 20px;
        }

        .card{
            width:100%;
            max-width:420px;
            background:var(--card);
            border-radius:var(--radius);
            padding:28px;
            box-shadow:0 10px 30px rgba(15,23,42,0.08);
            border:1px solid rgba(15,23,42,0.04);
        }

        .brand{
            display:flex;
            align-items:center;
            gap:12px;
            margin-bottom:6px;
        }
        .logo{
            width:42px;
            height:42px;
            border-radius:10px;
            background:linear-gradient(135deg,var(--accent1),var(--accent2));
            box-shadow:0 6px 18px rgba(108,92,231,0.18);
            display:inline-block;
        }
        h1{
            margin:0;
            font-size:20px;
            font-weight:600;
            letter-spacing: -0.2px;
        }
        p.lead{
            margin:8px 0 18px 0;
            color:var(--muted);
            font-size:13px;
        }

        form .row{
            display:flex;
            gap:10px;
            flex-wrap:wrap;
        }

        .form-group{
            width:100%;
            margin-bottom:12px;
        }

        label{
            display:block;
            font-size:13px;
            color:#0f172a;
            margin-bottom:6px;
            font-weight:600;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"]{
            width:100%;
            padding:12px 14px;
            border-radius:10px;
            border:1px solid #e6e9ef;
            background:#fff;
            font-size:14px;
            color:#0f172a;
            transition:box-shadow .18s ease, border-color .18s ease, transform .08s ease;
            outline:none;
        }

        input::placeholder{ color:#9aa4b2; font-weight:300; }

        input:focus{
            border-color: rgba(108,92,231,0.7);
            box-shadow: 0 6px 18px rgba(108,92,231,0.08);
            transform: translateY(-1px);
        }

        .actions{
            display:flex;
            align-items:center;
            justify-content:space-between;
            gap:12px;
            margin-top:12px;
        }

        button[type="submit"]{
            background:linear-gradient(90deg,var(--accent1),var(--accent2));
            color:white;
            border:0;
            padding:12px 18px;
            border-radius:10px;
            cursor:pointer;
            font-weight:600;
            box-shadow:0 8px 24px rgba(0,180,216,0.12);
            transition:transform .12s ease, box-shadow .12s ease, opacity .12s ease;
        }

        button[type="submit"]:hover{ transform:translateY(-3px); }
        button[type="submit"]:active{ transform:translateY(-1px); }

        .muted{
            color:var(--muted);
            font-size:13px;
        }

        .error{
            margin-top:14px;
            padding:10px 12px;
            border-radius:8px;
            background: #fff5f5;
            color:var(--danger);
            border:1px solid rgba(239,68,68,0.12);
            font-weight:600;
            font-size:13px;
        }

        .show-toggle{
            display:flex;
            align-items:center;
            gap:8px;
            color:var(--muted);
            font-size:13px;
        }

        @media (max-width:480px){
            .card{ padding:20px; }
            h1{ font-size:18px; }
        }
    </style>
</head>
<body>
    <div class="wrap">
        <main class="card" role="main">
            <div class="brand">
                <!-- <span class="logo" aria-hidden="true"></span> -->
                <div>
                    <h1>Add Nurse</h1>
                    <p class="lead">Create nurse account with secure password and contact details.</p>
                </div>
            </div>

            <form action="/admin/add/nurse" method="post" novalidate>
                <div class="form-group">
                    <label for="nurseName">Full name</label>
                    <input type="text" name="name" id="nurseName" placeholder="Enter nurse name" required />
                </div>

                <div class="form-group">
                    <label for="nurseEmail">Email</label>
                    <input type="email" name="email" id="nurseEmail" placeholder="name@example.com" required />
                </div>

                <div class="row">
                    <div class="form-group" style="flex:1 1 48%;">
                        <label for="nursePhone">Phone</label>
                        <input type="text" name="phoneNumber" id="nursePhone" placeholder="234 561 672 6723" inputmode="tel" />
                    </div>

                    <div class="form-group" style="flex:1 1 48%;">
                        <label for="nursePassword">Password</label>
                        <input type="password" name="password" id="nursePassword" placeholder="Create a password" required />
                    </div>

                    <div class="form-group" style="flex:1 1 48%;">
                        <label for="cnursePassword">Confirm password</label>
                        <input type="password" name="confirmPassword" id="cnursePassword" placeholder="Confirm password" required />
                    </div>
                </div>

                <div class="actions">
                    <div class="show-toggle">
                        <input type="checkbox" id="togglePw" aria-label="Show passwords" />
                        <label for="togglePw">Show passwords</label>
                    </div>

                    <button type="submit">Add Nurse</button>
                </div>

                <% if (error) { %>
                    <div class="error" role="alert"><%= error %></div>
                <% } %>
            </form>
        </main>
    </div>

    <script>
        
        (function(){
            const toggle = document.getElementById('togglePw');
            const p1 = document.getElementById('nursePassword');
            const p2 = document.getElementById('cnursePassword');
            toggle.addEventListener('change', () => {
                const t = toggle.checked ? 'text' : 'password';
                p1.type = t;
                p2.type = t;
            });
        })();
    </script>
</body>
</html>
